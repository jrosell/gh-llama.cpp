name: Llama Inference

on:
  push:
    branches:
      - main # Or your default branch
    paths-ignore:
      - 'outputs/**' # Prevents infinite loops when the runner pushes new files

permissions:
  contents: write # Crucial permission to allow pushing files back to the repo

jobs:
  process-prompt:
    runs-on: ubuntu-latest # Standard 16GB RAM runner

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0 # Fetches history so git log can read the commit message

      - name: Get Last Commit Message
        id: commit_info
        run: |
          echo "msg<<EOF" >> $GITHUB_OUTPUT
          # Capture the full subject AND body/description of the latest commit
          git log -1 --pretty=%B >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Cache LLM Model
        id: cache-model
        uses: actions/cache@v5
        with:
          path: ./models
          key: gemma-2b-it-gguf-v3 # Incremented to clean up caches

      - name: Download Small Quantized Model (Gemma 2B)
        if: steps.cache-model.outputs.cache-hit != 'true'
        run: |
          mkdir -p ./models
          # Downloads the open, un-gated Bartowski Q4_K_M quant of Gemma 2B It
          curl -L -o ./models/gemma-2-2b-it-Q4_K_M.gguf \
            https://huggingface.co/Bartowski/gemma-2-2b-it-GGUF/resolve/main/gemma-2-2b-it-Q4_K_M.gguf

      - name: Install llama.cpp
        run: |
          # Download and install using explicit BASH and Non-Interactive flag
          NONINTERACTIVE=1 bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
          brew install llama.cpp

      - name: Generate AI Output via llama.cpp
        env:
          COMMIT_PROMPT: ${{ steps.commit_info.outputs.msg }}
        run: |
          eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
          mkdir -p outputs

          # Safety check to ensure llama-cli installed correctly
          if ! command -v llama-cli &> /dev/null; then
            echo "Error: llama-cli could not be found or initialized via Homebrew."
            exit 1
          fi

          # Verify the model file actually exists before running
          if [ ! -f "./models/gemma-2-2b-it-Q4_K_M.gguf" ]; then
            echo "Error: Model file not found in ./models/ directory."
            exit 1
          fi

          # Generate file name using current timestamp (YYYYMMDD_HHMMSS)
          TIMESTAMP=$(date +'%Y%m%d_%H%M%S')
          FILE_PATH="outputs/response_${TIMESTAMP}.md"

          echo "Running inference against prompt..."

          # Construct the full prompt securely, formatting the multi-line commit message
          FULL_PROMPT="<start_of_turn>user
          Take this entire commit message (including its title and body description) and expand it into a detailed technical log or action list:

          $COMMIT_PROMPT
          <end_of_turn>
          <start_of_turn>model
          "

          # Run the llama-cli executable
          llama-cli \
            -m ./models/gemma-2-2b-it-Q4_K_M.gguf \
            -p "$FULL_PROMPT" \
            -t 4 \
            -n 256 \
            --verbosity -1 > "$FILE_PATH"

          echo "Generated file: $FILE_PATH"

      - name: Commit and Push Markdown File
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: add AI generated log from commit [skip ci]"
          file_pattern: 'outputs/*.md'
